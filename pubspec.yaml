name: dart_shopping_cart_task
description: Dart 3 practice with records, patterns, interfaces, and value equality.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  collection: ^1.19.1

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
